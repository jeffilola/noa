import { DashboardPlaceholder } from '@/components/dashboard/dashboard-placeholder';

export default function OrgCredentialsPage() {
  return (
    <DashboardPlaceholder
      title="Credential Inventory"
      description="View organization credential visibility. Noa does not issue credentials — provisioning is requested from connected providers."
    />
  );
}
