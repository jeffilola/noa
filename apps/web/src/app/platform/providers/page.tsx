import { DashboardPlaceholder } from '@/components/dashboard/dashboard-placeholder';

export default function PlatformProvidersPage() {
  return (
    <DashboardPlaceholder
      title="Providers"
      description="Manage the global credential provider catalog."
    />
  );
}
