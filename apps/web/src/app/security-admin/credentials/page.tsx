import { DashboardPlaceholder } from '@/components/dashboard/dashboard-placeholder';

export default function SecurityCredentialsPage() {
  return (
    <DashboardPlaceholder
      title="Credential Monitoring"
      description="Monitor credential state across the organization. Physical access configuration remains with PACS providers."
    />
  );
}
