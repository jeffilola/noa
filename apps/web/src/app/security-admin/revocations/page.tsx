import { DashboardPlaceholder } from '@/components/dashboard/dashboard-placeholder';

export default function SecurityRevocationsPage() {
  return (
    <DashboardPlaceholder
      title="Revocations"
      description="Suspend or revoke credential lifecycle state. Noa orchestrates state — issuance stays with providers."
    />
  );
}
