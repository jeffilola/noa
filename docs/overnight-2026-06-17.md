# Overnight sprint status - 2026-06-17

Generated by the overnight sprint agent for M7-M11 review preparation.

## Branch and PR status

Focused milestone branches are pushed and based on `origin/main`, but the PR automation tool rejected opening PRs from `feature/*` branches because this run is scoped to `cursor/noa-milestone-preparation-ea32`.

| Milestone | Branch | Review link | Issue status |
|-----------|--------|-------------|--------------|
| M7: Learning & compliance records | `feature/m7-learning-records` | https://github.com/jeffilola/noa/compare/main...feature/m7-learning-records | #52-#56 open |
| M8: Wallet pass preview | `feature/m8-wallet-pass-preview` | https://github.com/jeffilola/noa/compare/main...feature/m8-wallet-pass-preview | #69 open |
| M9: Platform admin org list | `feature/m9-platform-org-list` | https://github.com/jeffilola/noa/compare/main...feature/m9-platform-org-list | #70 open |
| M10: Integration admin test-mode stub | `feature/m10-integration-admin-stub` | https://github.com/jeffilola/noa/compare/main...feature/m10-integration-admin-stub | #71 open |
| M11: CI quality split | `feature/m11-ci-quality-split` | https://github.com/jeffilola/noa/compare/main...feature/m11-ci-quality-split | #72 open |

## Automated validation

Environment caveat: Docker is not running in Cursor Cloud, so commands that require the local Postgres container cannot complete here.

| Scope | Command | Result |
|-------|---------|--------|
| M7 | `pnpm qa:prepare` | Failed: Docker is not running |
| M7 | `pnpm --filter @noa/api test` | Passed; 9 DB-backed tests skipped because database unavailable |
| M7 | `pnpm --filter @noa/web build` | Passed |
| M8 | `pnpm --filter @noa/web build` | Passed |
| M9 | `pnpm --filter @noa/api test` | Passed; 9 DB-backed tests skipped because database unavailable |
| M9 | `pnpm --filter @noa/web build` | Passed |
| M10 | `pnpm --filter @noa/api test` | Passed; 2 validation tests passed, 8 DB-backed tests skipped because database unavailable |
| M10 | `pnpm --filter @noa/web build` | Passed |
| M11 | `pnpm lint` | Passed |
| M11 | `pnpm build` | Passed |
| M11 | `pnpm test` | Passed; DB-backed API tests skipped because database unavailable |

## M7 closeout notes

- `ensureComplianceRecordsForUser` is wired into the non-production demo bootstrap through `ensureCombinedDemoForClerkUser`.
- The Clerk auth guard calls the combined bootstrap on authenticated API requests, including org routes used by the access decision panel.
- The org member access page fetches compliance records from `/organizations/:id/users/:userId/compliance-records` and passes them into `OrgAccessDecisionPanel`.
- `docs/backlog.md`, `docs/sprint-planning.md`, `docs/m7-testing.md`, and `docs/demos/2026-06-11-m7.md` are present on the M7 branch.

## Manual E2E checklist for morning review

Prioritize M7 first because it is the closeout milestone and depends on Docker/Postgres-backed local data.

### M7: Learning records for access decisions

- [ ] Run `pnpm qa:prepare` locally with Docker/Postgres running.
- [ ] Sign in with the Clerk user configured as `DEMO_CLERK_USER_ID`.
- [ ] Open `/org/access` and confirm recent site access events are visible.
- [ ] Open `/org/users`, choose the demo member, and open the member access view.
- [ ] Confirm the access decision panel shows identity verified from the signed-in user record.
- [ ] Confirm workforce status comes from the active organization membership.
- [ ] Confirm training and compliance show the seeded `Site safety orientation` record.
- [ ] Confirm credential status comes from the active PACS/Noa credential assignment.
- [ ] Confirm last site access comes from access events.
- [ ] Confirm certification shows the seeded `Electrical safety certification` record.
- [ ] Re-run `pnpm qa:prepare`, refresh the member page, and confirm records remain stable and are not duplicated.
- [ ] Temporarily stop the API, refresh the member page, and confirm the API offline banner appears.

### M8: Wallet pass preview

- [ ] Open `/user/wallet`.
- [ ] Confirm Apple Wallet and Google Wallet preview placeholders render.
- [ ] Confirm no real issuance flow, provider key, QR code, or pass download is exposed.
- [ ] Confirm `/user` links to the wallet preview.

### M9: Platform admin org list

- [ ] Sign in as a platform admin.
- [ ] Open `/platform/organizations`.
- [ ] Confirm organizations list with member, credential, and provider counts.
- [ ] Search by organization name or slug and confirm results filter correctly.
- [ ] Confirm a non-platform user cannot access platform organization data.

### M10: Integration admin stub

- [ ] Sign in as an integration admin.
- [ ] Open `/integrations-admin/providers`.
- [ ] Submit a test-mode HTTPS provider URL and confirm validation succeeds.
- [ ] Submit a non-HTTPS URL and confirm validation fails.
- [ ] Confirm no live provider keys or secrets are requested or stored.

### M11: CI quality split

- [ ] Open the M11 branch workflow diff for `.github/workflows/ci.yml`.
- [ ] Confirm the lint job runs `pnpm lint`.
- [ ] Confirm the build/test job runs database generation, migrations, `pnpm build`, and `pnpm test`.
- [ ] After PR creation, confirm both jobs appear separately in GitHub Checks.

## Follow-ups

- Manually open focused PRs from the five `feature/*` branches if the automation tool remains scoped to `cursor/noa-milestone-preparation-ea32`.
- Leave issues and milestones open until the focused PRs are ready for human review.
- Re-run M7/M9/M10 DB-backed integration tests in a local Docker-backed environment before merge.
