# Overnight milestone status - 2026-08-13

Generated by the overnight sprint automation for the M7 review handoff.

## Executive summary

- The sprint prompt is stale for M8-M11: issues #69-#72 are closed and the implementation/testing docs are already on `main` from PR #57.
- M7 issues #52-#56 are closed; the active M7 review item remains PR #99, which restores the holder `/user/compliance` page and dev bootstrap coverage.
- This run is limited to the configured branch `cursor/noa-milestone-preparation-c167`; no feature branches were pushed and no issue or milestone state was changed.
- `manageCheckRun` is not available in the configured automation tools for this run, so aggregate status is recorded in this document and PR comments.

## PR and milestone status

| Milestone | Status | Review artifact |
|-----------|--------|-----------------|
| M7: Learning & compliance records | Closed issues #52-#56; active follow-up open for human review | PR #99: https://github.com/jeffilola/noa/pull/99 |
| M8: Wallet pass preview | Closed issue #69; merged to `main` | PR #57: https://github.com/jeffilola/noa/pull/57 |
| M9: Platform org list | Closed issue #70; merged to `main` | PR #57: https://github.com/jeffilola/noa/pull/57 |
| M10: Integration admin stub | Closed issue #71; merged to `main` | PR #57: https://github.com/jeffilola/noa/pull/57 |
| M11: CI quality split | Closed issue #72; merged to `main` | PR #57: https://github.com/jeffilola/noa/pull/57 |
| 2026-08-13 overnight handoff | Status docs only | PR #126: https://github.com/jeffilola/noa/pull/126 |

Open roadmap issues remain M12-M16 (#73-#77). I did not create duplicate M8-M11 milestones or issues because those shipped artifacts already exist and the available GitHub CLI is read-only in this environment.

## Automated validation

Current branch (`cursor/noa-milestone-preparation-c167`, docs-only on top of `main`):

| Command | Result | Notes |
|---------|--------|-------|
| `pnpm qa:prepare` | Failed | Docker is not running in the cloud VM; rerun locally with Docker/Postgres. |
| `pnpm install --frozen-lockfile` | Passed | Installed workspace dependencies after pnpm reported missing `node_modules`. |
| `pnpm --filter @noa/api test` | Passed | 12 tests discovered; 2 passed and 10 DB-backed tests skipped because Postgres is unavailable. |
| `pnpm --filter @noa/web build` | Passed | Next build completed for current `main` routes. |

Active M7 follow-up branch (PR #99, `cursor/noa-milestone-preparation-01f8`):

| Command | Result | Notes |
|---------|--------|-------|
| `pnpm install --frozen-lockfile` | Passed | Lockfile install succeeded in a detached temporary worktree. |
| `pnpm qa:prepare` | Failed | Docker is not running in the cloud VM; local Docker/Postgres is still required for bootstrap/E2E. |
| `pnpm --filter @noa/api test` | Passed | 13 tests discovered; 2 passed and 11 DB-backed tests skipped, including the signed-in holder compliance path, because Postgres is unavailable. |
| `pnpm --filter @noa/web build` | Passed | Next build completed and included `/user/compliance`. |

## Morning review checklist

Prioritize the M7 paths from `docs/m7-testing.md`:

1. Start local Postgres and dev bootstrap: `docker compose up -d postgres && pnpm qa:prepare && pnpm qa:dev`.
2. Sign in as the demo Clerk user from `packages/database/.env`.
3. Switch to **Organization Admin**.
4. Open **Users** and click **Access view** on the demo member row.
5. Confirm the access decision panel shows:
   - Site safety orientation or equivalent training record with a date.
   - Electrical safety certification with an expiry around 2027.
   - Identity, credential, and last site access data still populated.
6. Toggle dark mode and confirm the panel remains readable.
7. Switch to **Identity Holder**.
8. Open **Training & certs** or `/user/compliance`.
9. Confirm the same training and certification records appear in the holder table.
10. Click **Refresh list** and confirm the table reloads without error.

Recommended follow-up checks:

- M8: `/user/wallet` shows Apple/Google preview cards marked preview-only.
- M9: `/platform/organizations` search/filter works and shows the API-offline banner when the API is stopped.
- M10: `/integrations-admin/providers` accepts HTTPS test URLs and rejects HTTP URLs without saving live provider keys.
- M11: a PR shows separate `lint` and `build` checks, with `build` still running Postgres-backed migrations/tests.
